{
  "name": "app-shell",
  "private": true,
  "version": "0.0.1",
  "description": "Application Shell for web components",
  "scripts": {
    "build": "stencil build --dev",
    "start": "stencil build --dev --watch --serve",
    "test": "stencil test --spec --e2e",
    "test.watch": "stencil test --spec --e2e --watchAll",
    "generate": "stencil generate"
  },
  "devDependencies": {
    "@stencil/core": "^2.9.0",
    "@types/jest": "^26.0.24",
    "jest": "^26.6.3",
    "jest-cli": "^26.6.3",
    "puppeteer": "^10.0.0",
    "stencil-router-v2": "^0.6.0"
  },
  "license": "MIT",
  "dependencies": {
    "@material/mwc-drawer": "^0.25.2",
    "@material/mwc-icon-button": "^0.25.2",
    "@material/mwc-top-app-bar-fixed": "^0.25.2"
  }
}
