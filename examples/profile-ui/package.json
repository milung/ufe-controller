{
  "name": "profile-ui",
  "version": "0.0.1",
  "description": "Stencil Component Starter",
  
  "scripts": {
    "build": "stencil build --docs",
    "start": "stencil build --dev --watch --serve",
    "test": "stencil test --spec --e2e",
    "test.watch": "stencil test --spec --e2e --watchAll",
    "generate": "stencil generate",
    "dockerize": "docker build -t milung/ufe-profile-ui ."

  },
  "dependencies": {
    "@stencil/core": "^2.7.0"
  },
  "devDependencies": {
    "@types/jest": "^26.0.24",
    "crypto-js": "^4.1.1",
    "jest": "^26.6.3",
    "jest-cli": "^26.6.3",
    "md5": "^2.3.0",
    "puppeteer": "^10.0.0"
  }
}
